## 2.8.1 - Nanguoli

### 功能更新

- 默认对话模板新增历史记录（Backlog）面板，可回顾对话、选项与全屏文字
- 支持回退到上一句，也可点击历史记录返回对应的剧情位置，继续播放或重新选择分支
- 为角色、背景、音频、镜头、脚本、变量、编译、资源与成就等错误提供稳定编号
- 报错信息包含出错的函数、文件、资源和剧本行号；单行剧本编译也会保留来源路径，便于定位问题
- 新增 `KonadoResult`，统一表达成功数据和结构化错误，便于调用方处理执行结果

### 漏洞修复

- 修复 `actor move` 后角色位置未保存，导致读档或回退时位置恢复错误的问题
- 修复以空角色名查找舞台角色时触发引擎报错的问题

## 2.8.0 - Nanguoli

### 新功能

- KonadoScript 新增命名参数，可单独设置每句文本的打字速度，以及演员、背景和镜头的过渡时间
- 对话语法统一支持静态演员、演员变量和可插值的文本署名
- 新增稳定指令 ID、剧情执行历史、检查点和回滚 API

### 改进

- 重构 KonadoScript 编译器和运行时，使复杂条件、分支及跨脚本跳转更加可靠
- 大幅降低大型剧本的内存占用并提升运行速度，超长脚本支持分时执行
- 完善异步指令的取消与状态管理，切换镜头或停止剧情时不会残留旧任务
- 国际化改用 Godot 原生 `TranslationServer` 和 `.po` 资源，语言切换、界面翻译与剧情本地化使用同一语言状态
- 修复屏幕文本播放完最后一行或切换剧情后仍然显示，遮挡后续内容的问题
- 精简 Godot 新建节点列表，隐藏仅供内置模板使用的实现节点
- 修复 Konado.NET 启用流程并完善多语言 API 文档
- 改进编译错误提示，并统一插件目录和命名

## 2.7.4 - Wontons

### 漏洞修复

- 修复成就面板、设置面板和系统提示可能被对话界面遮挡的问题
- 修复成就面板在暂停状态下无法关闭，以及关闭后键盘焦点未恢复的问题
- 修复 `KonadoCamera2D` 机位标记可能抢占活动相机，导致复杂背景转场画面偏移的问题
- 修复 `hidetextbox` 隐藏后残留上一句内容，下次显示时可能短暂闪现的问题
- 修复静态立绘状态交融时闪烁或意外透出背景的问题
- 修复无效、异步、已被取代或演员退出后的状态请求可能导致画面与存档数据不一致或剧情等待的问题
- 修复无效自定义角色场景或演员动作层会破坏当前有效角色的问题
- 修复自定义角色场景的初始状态可能读取到尚未布局的尺寸，以及越界立绘在状态交融时被裁切的问题
- 修复重复请求正在移动的演员到同一位置时，剧情可能在移动完成前继续的问题

### 其他改进

- 增加可配置的成就面板与解锁通知层级，并明确内置界面的层级约定
- 新增语义明确的 `dismiss_dialogue_box*()` API，用于隐藏并清理对话框内容
- 静态立绘使用预乘 Alpha 交融，动态媒体和复杂自定义场景自动使用安全淡变
- 完善成就界面和复杂背景镜头的运行时回归测试

## 2.7.3 - Wontons

### 漏洞修复

- 修复复杂背景在内置转场中显示不完整的问题
- 修复取消或替换背景转场后，旧任务可能继续运行的问题
- 修复 KonadoScript 错误提示闪烁、操作按钮难以点击的问题

### 其他改进

- 背景转场默认完整捕获场景，静态背景可选用 `DIRECT_TEXTURE` 快速路径
- 完善播放音频 API 文档

## 2.7.2 - Wontons

### 漏洞修复

- 修复当前语言没有对应 KonadoScript 时，自动回退到默认剧本却显示异常的问题
- 修复连续播放不同镜头时，对话框淡入阶段可能短暂显示上一镜头角色名和文本的问题
- 修复未显式写 `end` 的镜头仅进入关闭状态，未完整清理运行态且不触发 `shot_end` 的问题
- 修复旧节点或上一镜头的自动播放计时器、指令完成信号、选项回调和相机动画可能继续推进或影响当前播放的问题
- 修复重复开始、重复停止或在 `shot_end` 回调中再次停止时可能重复执行生命周期操作的问题
- 修复在对话、音频、变量、成就或清理完成信号的回调中替换或停止镜头时，旧播放流程可能继续执行、覆盖新镜头状态或阻止新镜头停止的问题
- 修复运行时切换语言可能重复执行当前指令、重复绑定完成回调、错误触发打字完成信号，或在本地化剧本缺少当前节点 ID 时停止推进的问题
- 修复语音被中断后可能在下一段语音结束时错误触发完成信号、无法开始播放时持续等待，以及重复播放 BGM 会累积循环回调的问题；明确 `play_voice()` 为非阻塞接口，并新增可区分自然完成与中断的 `play_voice_and_wait()`
- 修复连续替换屏幕文本或切换语言时，旧动画可能清空或干扰新文本的问题
- 修复跳过打字动画时 `dialogue_line_end` 提前、重复触发，以及其回调重入可能重复推进的问题
- 修复关闭自动播放时可能意外推进当前对话的问题

## 2.7.1 - Wontons

### 漏洞修复

- 修复 `SetShot()` 设置的镜头被 `InitDialogue()` 覆盖的问题，GDScript 与 Konado.NET 现在使用一致的镜头生命周期
- 重构对话框显隐状态管理，修复淡出、`hidetextbox` 及对话结束后再次启动时文字不显示的问题
- 修复连续对白在对话框已显示时不更新，以及中断显隐动画后状态、文本节点和打字音效未正确恢复的问题
- 修复停止或重新初始化对话后残留旧的打字完成回调，以及背景转场备用节点和已禁用的打字音效播放器未正确释放的问题
- 修复淡入打字机跳过时可能重复触发完成信号的问题

### 其他改进

- 增加传统与淡入打字机模式、即时与动画显隐、连续对白、镜头切换和重复启动的运行时回归测试
- 更新 Konado.NET 示例及各语言 API 文档，明确 `SetShot()`、`InitDialogue()` 和 `StartDialogue()` 的调用关系

## 2.7 - Wontons

### 编辑器

- 将 `.ks` 集成到 Godot 原生脚本工作区，并新增组件树、多语言切换和版本化在线文档
- 新增共享语义模型与增量项目索引，统一提供高亮、诊断、补全、悬浮提示、导航、大纲、查找引用和安全重命名
- 将格式化、错误悬浮修复、项目符号导航和可回滚的项目级重命名直接集成到当前脚本编辑器
- 新增 KonadoScript 断点调试，支持源码定位、变量检查、继续、单步和下一行暂停
- 完善解析器错误恢复、原子保存和自动化测试，修复实时分析、编译边界及镜头缓动一致性问题

### 安全性

- 新增用户无感的剧本保护，为编译后的 KonadoScript 使用 AES-256 加密
- 使用独立派生的加密与认证密钥，并增加元数据认证、完整性校验和解压大小上限
- 新增导出包运行时验证，确认所有示例剧本均已加密、可加载且无法直接读取剧情明文

### 其他改进

- 统一使用 `KonadoScript` 产品名称
- 将文档与案例更新工作流升级至 Node.js 24
- 移除木兰宽松许可证 2.0 文本中重复的标题

## 2.6.1 - Ketchup

### 相机与演出

- 新增非阻塞 `asyncam` 指令，支持 `move`、`reset`、`shake` 和 `stop`，镜头动画运行时可继续推进对话
- 新增根据当前背景场景为演员混合背景色调的功能，并支持统一配置混合强度
- 新增 `actor change` 平滑淡出、切换状态、淡入转场，可在 `KND_ActingInterface` 中配置或关闭
- 新增演员状态切换的取消与完成管理，重叠请求或无效状态不会阻塞对话流程

### 国际化

- 将 `KND_I18n` 重构为可用的运行时服务，统一管理 UI 翻译、语言持久化、本地化 `.ks` 查找和对话重载通知
- 新增 `KND_LocaleCatalog`，统一管理内置与自定义语言、规范化语言码并迁移旧语言值
- 新增 `KND_LocalizedScriptLoader`，按完整语言码、基础语言和原始剧情文件依次回退
- 新增运行时切换语言后的稳定剧情位置恢复，优先按 `node_id` 定位当前节点

### Konado.NET

- 新增 `InternationalizationAPI`，支持从 C# 切换与规范化语言、注册翻译及加载本地化剧情
- 扩展 `DialogueManagerAPI`，增加明确的生命周期绑定、类型化信号、存档读档、本地化剧情重载和等待信号转发
- 新增 Konado.NET 的流水线测试

### 其他改进

- 将对话运行职责拆分为独立服务，并为未配置的可选系统增加安全回退
- 完善演员、背景、音频、打字机、成就、设置、Web 工具和编辑器集成的生命周期
- 使 Konado.NET 包装器与 GDScript 资源契约对齐，并支持包装 GDScript 资源子类
- 将最低支持版本及所有官方构建、测试环境统一升级至 Godot 4.7.1
- 新增 GDScript 检查与格式、GDScript 运行时架构、Konado.NET 编译与运行时、插件元数据、资源边界和文档资源路径检查
- 从 release 包中排除测试、技能及 Konado 编辑器专用资源
- 新增拉取请求提交身份检查，拦截使用被禁止 AI 署名邮箱的提交
- 移除每日定时构建工作流及其预发布流程
- 更新 2.6 文档，同步各语言的本地化术语、示例、演示资源及 GodotHub 图片链接
- 新增异步运镜、演员状态淡变、运行时剧情本地化及新版 Konado.NET API 文档
- 移除旧 Konado.NET `DialogueActor` 包装器，改为直接使用基于资源的对话属性

### 漏洞修复

- 修复各语言示例剧情结构不一致的问题，所有内置语言现在使用一致的对话结构与稳定节点标识
- 修复 `zh-TW`、`zh-CN`、`tc` 等旧语言值，读取时会迁移为规范语言码
- 修复演员状态转场可能复制视觉节点、丢失原透明度、应用已被替换的状态或重复结束的问题
- 修复无效演员和背景资源的转场失败路径，确保异常情况下仍能安全释放对话流程
- 修复演员背景色调默认强度与配置行为不一致的问题
- 修复存档写入流程，使用临时文件与备份文件，避免不完整写入覆盖有效存档
- 修复设置持久化流程，仅在配置成功写入后提交内存中的新值
- 修复 KS 语句异常、演员指令参数和条件分支控制流的解析与分析问题

## 2.6 - Ketchup

Konado 2.6 版本正式发布。本版本代号为 Ketchup，聚焦于实时演出增强与脚本表达能力提升，新增相机系统（移动、复位、晃动）、角色动画系统（滑入退场）、运行时国际化、NVL 全屏文本、对话框显隐控制、等待外部信号等核心功能，并推出 Konado Showcase 展示页，进一步完善视觉小说开发的全链路能力。

### 新增

#### 相机系统

- 新增 `cam move`、`cam reset`、`cam shake` 三种镜头指令，支持脚本级相机控制
- 新增 `KonadoCameraManager` 相机管理器节点，管理多相机目标与过渡动画
- 新增 `cam shake` 镜头晃动功能，支持自定义晃动时长
- 支持 tween 动画类型与时长参数，实现平滑镜头过渡

#### 角色动画系统

- 重构角色动画系统，新增角色滑入退场动画
- 新增 `enter_exit_anim_config.gd` 动画配置资源，支持自定义入场/退场动画时长与曲线
- 将角色动画逻辑集中到 `animated_actor_layer.gd`，统一管理

#### NVL 屏幕文本（Overlay 正文）

- 新增 `screentext` 脚本指令，支持全屏 NVL 正文显示
- 新增 `KND_ScreenText` 场景与组件，实现逐行淡入动画
- 支持独立 RichTextLabel 逐行渲染，自定义行间距、左间距、上间距
- 每行显示完成后闪烁三角箭头指示器，提示点击播放下一行
- 提供 `display_finished` 等回调信号，与对话流程无缝集成

#### 对话框显隐控制

- 新增 `showtextbox` 脚本指令，支持自定义淡入动画时长控制对话框显示
- 新增 `hidetextbox` 脚本指令，支持自定义淡出动画时长控制对话框隐藏
- 时长参数设为 `0.0` 时禁用动画，立即显示/隐藏
- 在 `KND_DialogueBox` 中新增 `show_dialogue_box_with_duration()`、`hide_dialogue_box_with_duration()` 方法

#### 等待外部信号

- 新增 `waitsignal` 脚本指令，在对话流程中暂停等待指定外部信号触发
- 新增 `emit_wait_signal(signal_name: String)` 方法，供外部代码触发信号继续对话
- 适用于过场动画、小游戏、自定义交互等场景

#### 运行时国际化

- 初步实现运行时脚本国际化支持（i18n runtime），支持在游戏运行时动态切换语言
- 新增 `KND_I18n` 国际化服务节点，提供注册与翻译接口
- 对话管理器支持加载本地化后的对话资源

#### 语音与音频

- 新增语音进度显示功能，在对话框中显示配音播放进度
- 新增 `voice_progress_display.tscn` 进度显示模板场景
- 支持通过对话框节点关闭进度显示

#### 文档与展示

- 新增 Konado Showcase 展示页生成器，自动抓取并展示使用 Konado 制作的游戏

#### 其他

- 更换项目多许可证，更新文档说明
- 将 `middle` 主题改为 `default` 继承场景，确保继承关系正确

### 语法变更

- **新增 `screentext` 命令**：NVL 全屏正文显示
  ```ks
  screentext {
      "这是全屏文本第一行"
      "这是全屏文本第二行"
  }
  ```

- **新增 `showtextbox` / `hidetextbox` 命令**：对话框显隐控制
  ```ks
  showtextbox 1.0    # 用 1 秒淡入动画显示对话框
  hidetextbox 0.5    # 用 0.5 秒淡出动画隐藏对话框
  showtextbox 0.0    # 禁用动画，立即显示
  ```

- **新增 `waitsignal` 命令**：等待外部信号
  ```ks
  waitsignal "over"       # 等待名为 "over" 的外部信号
  waitsignal minigame_done # 标识符形式
  ```

- **新增 `cam` 命令扩展**：镜头晃动
  ```ks
  cam shake          # 默认时长晃动
  cam shake 2.0      # 晃动 2 秒
  cam move target linear 1.0  # 线性过渡 1 秒
  cam reset ease_in_out 2.0   # 缓入缓出过渡 2 秒复位
  ```

### 修复

- 修复主菜单退出按钮在非编辑器环境下的节点路径问题
- 修复 `middle` 主题作为默认主题的继承场景配置

### 改进

- 重构角色动画系统，动画逻辑集中管理，便于扩展
- 语音进度显示支持开关配置，灵活适配不同项目需求
- Konado Showcase 自动生成，降低社区展示维护成本

### 兼容性提示

- 建议使用 Godot 4.7 或更高版本。
- 不支持直接从 2.4 升级到 2.6 跨版本升级，建议重新迁移项目
- 运行时国际化功能需要额外配置 `KND_I18n` 节点，非强制功能。

## 2.5 - Diguoji

Konado 2.5 版本正式发布。本版本代号为 Diguoji，聚焦于游戏流程完整性与开发体验提升，新增快速存档读档功能、游戏主菜单界面、角色场景化架构、背景过渡效果，并推出 VSCode 语法高亮扩展与编辑器技能包，进一步完善开箱即用的开发体验。

### 新增

#### 存档系统

- 新增快速保存/快速读取功能，在对话框模板中添加 QuickSave 和 QuickLoad 按钮
- 在存档 UI 组件中添加快速存档标识，槽位 0 标记为快速保存槽
- 在对话管理器中实现 `_on_quick_save_pressed()` 和 `_on_quick_load_pressed()` 方法
- 快速读取前弹出确认对话框，防止误操作丢失未保存进度
- 添加轻量提示（toast）功能，显示保存/读取操作结果

#### 游戏界面

- 新增游戏启动主菜单界面（`main.tscn`），包含开始游戏、读取存档、设置、退出按钮
- 主菜单背景使用主题背景图，按钮采用统一主题样式
- Web 平台自动隐藏退出按钮，适配浏览器环境

#### 角色系统

- 新增角色场景作为角色可选的立绘形式，支持任何节点形式的角色场景
- 新增 `motion` 命令执行舞台动作
- 在 `ActorMotionLayer` 中支持自定义的动画，并附加一个示例动画
- 将 motion 集中在 `actor_motion_layer` 中，不再硬编码动画

#### 背景系统

- 背景都转为场景，支持在场景中使用 shader
- 新增背景过渡视觉效果"眨眼"
- 新增背景过渡演示场景（`demo_06_bg_effects.ks`）和演示图片
- 新增背景过渡效果无效时提示

#### 开发工具

- 新增 VSCode Konado 脚本语法高亮扩展，支持 `.ks` 文件的语法着色
- 新增 VSCode 工作区扩展推荐配置（`.vscode/extensions.json`）
- 优化 KS 语法插件内部配置结构
- 新增 Konado DSL 编辑器增强技能包（`skills/konado-script`）
- 新增 `.marketplace.json` 配置文件，用于注册 konado-script-skill 插件及其技能路径

#### 文档

- 新增场景化后的文档
- 新增版本化文档结构
- 更新 README 文档链接，内联贡献者信息

#### 语法变更

- **新增 `actor motion` 命令**：用于执行角色舞台动作
  ```ks
  # 执行内置动作
  actor motion Kona shake
  actor motion Kona jump
  actor motion Kona bounce
  
  # 动作定义在 actor_motion_layer.tscn 的 AnimationPlayer 中
  ```

- **`actor show` 命令简化**：移除了多余的 `y` 坐标、`scale` 和 `mirror` 参数
  ```ks
  # 2.5 语法（简化）
  actor show Kona 正常 at 3
  
  # 旧语法（已移除）
  # actor show Kona 正常 at 2 5 scale 0.3 mirror
  ```

- **`actor change` 命令**：修改角色状态（表情）
  ```ks
  actor change Kona 害羞
  actor change Kona 惊讶
  ```

- **背景过渡效果扩展**：新增"眨眼"等视觉效果
  ```ks
  background bg1 fade    # 淡入淡出
  background bg1 windmill # 风车效果
  background bg1 blink    # 眨眼效果（新增）
  ```

- **重复 `actor show` 兼容**：允许对已显示的角色重复使用 `show` 命令，复用已有节点并切换到新状态
  ```ks
  actor show Kona 正常 at 3
  actor show Kona 害羞 at 2  # 复用节点，切换状态和位置
  ```

### 修复

- 修复成就关闭异常 bug
- 修复条件分支 continue 清理问题，修正 if 分支不跳转的问题
- 修复版本切换器选择稳定性问题
- 修复 actor show 复用问题，允许重复的 actor show 语句，复用已有节点做新状态
- 修复等待 actor shown 信号问题
- 修复批量 actor 舞台位置更新问题
- 修复变量系统演示场景无法进行的问题

### 改进

- 完善文档细节，补充快速保存、快速读取文档说明
- 重构插件 README，新增兼容编辑器说明，优化安装步骤描述
- 优化 KS 语法插件 README 命令描述
- 更新文档站版本配置，新增 2.5 版本文档分支
- 删除多余的 y 坐标，简化角色定位参数
- 优化 Demo 场景：更新脚本、资源和 .gitignore
- 添加 Tripo 致谢 Logo
- 更新社区项目列表，添加"雨夜重逢"游戏和 Akonado 派生项目

### 移除

- 删除图片切换表情的兼容，换为只使用场景
- 删除旧的图片格式不再兼容，转为场景中完成动作切换

### 兼容性提示

- 建议使用 Godot 4.7 或更高版本。
- 2.5 版本引入了新的主菜单场景，建议在项目中配置正确的启动场景。
- 背景和角色立绘已转为场景形式，旧的图片格式不再兼容，需要迁移到场景配置。
- 删除了多余的 y 坐标参数，角色定位仅保留横向网格位置。

## 2.4.5 LTS - Macaron

Konado 2.4.5 版本正式发布。本版本为 2.4 系列的长期支持（LTS）维护更新，聚焦于 KS 脚本编译管线重构与编辑器体验增强，实现了完整的编译链路并添加了实用的编辑器工具功能。

### 新增

#### KS 编译器

- 重构 KS 编译器，新增词法分析器、语法分析器、语义分析器、代码生成器完整编译链路
- 为 KS 脚本文件添加编辑器悬浮提示工具功能，用于显示脚本行数、对话数量与依赖角色

### 移除

- 删除废弃的旧对话场景文件 `konado_dialogue.tscn`，该文件为2.3旧版对话系统的残留文件，不再被项目使用

### 兼容性提示

- 移除了废弃残留的对话场景，请使用新的`knd_dialogue_box_middle.tscn`和`knd_dialogue_box_left.tscn`对话场景模板，这回导致依赖旧对话场景的工程出现异常，因此建议备份后升级，或手动补充缺失的对话场景，完成迁移后即可正常运行。
- 建议使用 Godot 4.6.2 或更高版本。

## 2.4.4 LTS - Macaron

Konado 2.4.4 版本正式发布。本版本为 2.4 系列的长期支持（LTS）维护更新，聚焦于 KS 脚本解释器的选项解析修复，解决分支内选项显示与跳转异常问题，进一步提升脚本解析的稳定性与准确性。

### 修复

#### KS 解释器

- 修复2.3残留的旧选项语法解析，限制每行只能包含一个选项， 移除`choice "文本1" -> tag1 "文本2" -> tag2` 格式，强制使用规范的 `choice "文本" -> 标签` 单行单选项写法。
- 修复分支内选项跳转目标解析，新增后处理步骤将分支内选项的 `next_id` 从标签名转换为节点 ID，解决分支内选项跳转失败的问题。
- 修复分支内连续 `choice` 行合并逻辑，使分支内的多个 `choice` 行正确合并为一个选项节点，解决分支内只显示单个选项的问题。

### 新增

#### 示例与资源

- 新增 `demo_choice_test.ks` 选项系统测试剧本，演示主线多选项、分支内多选项、嵌套选项跳转等场景，便于验证选项解析功能。

### 兼容性提示

- 2.4.4 强制要求每行只能有一个选项，旧脚本中若存在单行多选项写法需拆分为多行，这个可能造成破坏性影响。
- 建议使用 Godot 4.6.2 或更高版本。


## 2.4.3 LTS - Macaron

Konado 2.4.3 版本正式发布。本版本为 2.4 系列的长期支持（LTS）维护更新，聚焦于编辑器交互体验修复、对话播放流程完善以及示例资源的补齐，进一步提升开箱即用的稳定性与易用性。

### 修复

#### 表演系统

- 移除场景内的ShaderMaterial，改为在ready函数中动态创建并赋值给背景节点，统一材质初始化逻辑，防止在场景加载时出现材质为空无法设置的问题。


## 2.4.2 LTS - Macaron

Konado 2.4.2 版本正式发布。本版本为 2.4 系列的长期支持（LTS）维护更新，聚焦于编辑器交互体验修复、对话播放流程完善以及示例资源的补齐，进一步提升开箱即用的稳定性与易用性。

### 修复

#### 编辑器

- 修复 KS 编辑器显示逻辑，不再异常空白占用主屏幕区域。
- 修复编辑器 `_edit` 方法中的可见性控制，改用 `ks_dock.make_visible()` 替代错误的 `ks_editor.show()` 方式。

#### 对话系统

- 修复对话管理器自动播放逻辑，调整打字完成后的执行流程，将 `_process_next()` 移至正确分支，解决非等待配音场景下的流程跳转错误。
- 完善配音播放逻辑，重构 `_play_voice` 函数使其返回音频时长，优化自动播放与打字完成后配音等待的时序协调。
- 修复自动播放设置加载时机，改为在对话管理器初始化时加载而非运行时按需读取。

### 改进

#### 对话管理器

- 新增当前对话为空的异常处理逻辑，避免空白对话导致流程卡死。
- 优化调试日志输出，提供更清晰的运行时状态提示，便于排查问题。

#### 示例与资源

- 补充 Demo 场景缺失的语音列表资源 `voice_list.tres`，包含示例语音条目。
- 将 `new_resource.tres` 重命名为 `character_list.tres`，规范资源命名。
- 补全 Demo 场景中角色列表、背景列表、BGM 列表和语音列表的资源引用。

### 兼容性提示

- 2.4.2 延续 2.4.1 的编辑器底部 Dock 布局，但调整了可见性控制逻辑。
- 建议使用 Godot 4.6.2 或更高版本。


## 2.4.1 LTS - Macaron

Konado 2.4.1 版本正式发布。本版本为 2.4 系列的长期支持（LTS）维护更新，相较于 2.4.0 版本，本次更新聚焦于编辑器的界面体验优化与核心功能完善，同时全面修复了社区用户反馈的各类问题，进一步提升了稳定性和易用性。

### 更新内容

- 新增KND_SettingsBridge设置桥接节点，实现对话设置访问
- 为对话管理器添加设置监听和设置按钮功能
- 在音频接口中接入音量同步逻辑

### 修复

#### 编辑器

- 修复主题和按钮样式，重置编辑器位置为底部，使其可以自由弹出，方便同时预览游戏画面和编辑对话。
- 修复编辑器面板最小高度为 300px，确保初始化时编辑器面板可见。
- 修复与 Godot 4.6 API 变化的兼容性问题。

### 改进

#### 主题、示例与资源

- 新增 `NotoSansSC-VF.otf` 和 `ResourceHanRoundedCN-Medium.ttf` 字体文件及对应的 SIL OFL 授权文档。
- 修复 `left_theme.tres` 和 `middle_theme.tres` 主题资源中的字体文件路径。

#### 文档

- 新增 `docs` 目录下的 `.gdignore` 忽略配置，防止Godot异常加载不需要的文档文件。
- 更新文档与语法高亮器指令。
- 优化多语言 Konado 项目描述。

### 兼容性提示

- 2.4.1 调整了编辑器面板位置为底部，因此建议先禁用旧版本的插件，退出项目完整更新后再重新启用，避免缓存问题。
- 由于Godot 4.6 API 变化，旧版本的Godot可能无法正常工作，需要升级到Godot 4.6或更高版本。
- 由于导入了新的字体文件，如果未生效，建议删除`.godot`目录下的字体资源文件缓存。

## 2.4.0 LTS - Macaron

Konado 2.4.0 是一个长期支持版本。相较 2.3，新版本将重点放在核心对话流程、变量与存档能力、可复用插件生态、模板资源和文档体系上，实现了功能性和稳定性的同步飞跃。

### 亮点

- 新增完整变量系统，支持持久变量、临时变量、变量插值和条件判断。
- 新增完整存档/读档系统，可保存对话状态、变量、音频、演员和背景状态。
- 新增淡入式打字机文本组件，支持 BBCode 富文本和 GPU 加速逐字符淡入。
- 新增 Konado Achievement、Konado Settings、Konado WebTool 三个独立插件。
- 文档站重构为中文、英文、繁体中文多语言结构，补齐 2.4 相关教程文档。
- 新增节点图编辑器（Beta），用可视化节点图组织对话流程、分支和跳转。

### 更新内容

#### 对话系统与脚本能力

- 新增 `addons/konado/graph_editor` 节点图编辑器模块：
  - `knd_graph_edit.gd`：可视化节点图编辑器。
  - `knd_graph_node_factory.gd`：对话节点工厂。
  - `knd_graph_converter.gd`：KS 脚本与节点图结构转换。
- 新增 `%变量名` 持久变量和 `$变量名` 临时变量。
- 新增变量操作语句：`set`、`add`、`sub`、`mul`、`div`。
- 新增对话文本变量插值，可在台词中直接显示 `%love`、`$score` 等变量值。
- 新增 `if / else / endif` 条件分支，支持 `==`、`!=`、`>`、`<`、`>=`、`<=`。
- 新增选项与分支跳转改进，优化 `choice`、`branch`、`jump_branch` 的解析和执行。
- 新增自定义信号指令 `signal <name>`，可由对话脚本触发外部游戏逻辑。
- 新增成就脚本指令示例，包括直接解锁、计数器进度和 flag 条件。
- 新增背景清空功能。
- 新增对话可见性检查能力。

#### 存档系统

- 新增 `KND_SaveSystem`，提供 `save_game()`、`load_game()`、`delete_save()`、`get_save_info()` 等接口。
- 新增 `KND_SaveData`，统一序列化对话、变量、音频、演员、背景和存档元数据。
- 新增自动存档开关和自动存档间隔配置。
- 新增存档策略配置，可选择是否保存对话状态、变量、音频、演员和背景。
- 更新存档 UI 组件，支持存档槽位、保存、读取、删除和预览信息。

#### 文本显示与音效

- 新增 `KND_TypewriterText` 淡入式打字机文本组件。
- 新增 `typewriter_fade.gdshader`，使用 CanvasItem shader 实现逐字符淡入。
- 新增 BBCode 解析支持，包含粗体、斜体、下划线、删除线、颜色和字号。
- 新增多行文本淡入支持。
- 新增打字机音效相关文档。

#### 插件

- 新增**成就系统**插件（`addons/konado_achievement`）：
  - JSON 配置成就数据。
  - 支持直接解锁、计数器、flag 条件和隐藏成就。
  - 提供成就弹窗、成就面板、进度统计和重置接口。
  - 支持自定义保存/加载后端以及外部平台 SDK 同步回调。
- 新增**设置系统**插件（`addons/konado_settings`）：
  - 通过 JSON 配置动态生成设置面板。
  - 内置音频、文本播放、显示等设置分类。
  - 支持滑块、开关、选项等 UI 项。
  - 支持按平台和构建类型过滤设置项。
- 新增**WebTool**插件（`addons/konado_webtool`）：
  - Web 导出环境下放行常用浏览器快捷键。
  - 支持 F12、F5、F11、Ctrl/Cmd 组合快捷键等配置。

#### 模板、示例与资源

- 新增左侧、中间布局的对话框与对话场景模板。
- 新增 `left_theme.tres`、`middle_theme.tres` 主题资源。
- 新增变量系统完整示例 `sample/demo/demo_03_variable.ks`。
- 新增 Konado 2.4 启动横幅。
- 新增 Kona 表情 GIF 资源。
- 新增新版角色立绘资源与立绘导入/裁切指导素材。
- 新增中文字体资源 `NotoSansSC-VF.otf` 和 `ResourceHanRoundedCN-Medium.ttf`。

### 文档

- 重构 VitePress 文档配置，引入侧边栏生成脚本 `genSidebar.ts`。
- 新增中文、英文、繁体中文多语言文档结构。
- 新增成就系统、设置系统、WebTool、Konado .NET API 文档。
- 新增变量系统、条件分支、自定义信号、打字机效果、打字机音效教程。
- 新增存档系统、背景转场、脚本高亮、日志、镜头与对话等核心教程。
- 新增社区贡献、文档贡献、反馈、资源和加入社区页面。
- 更新版本规划，2.4 代号为 Macaron，并标记为 LTS。

### 改进

- 将 Konado 主插件版本更新至 `2.4.0`。
- 重构 `KND_DialogueManager` 和 KS 解释器，支持变量、条件、分支和存档状态管理。
- 优化演员管理与存档系统的集成。
- 优化角色布局逻辑，角色图片以底部为基点显示在网格位置。
- 优化高亮逻辑，补充 BBCode 语法定义。
- 优化移动指令与示例资源。
- 优化 Konado Settings 设置面板 UI，并清理冗余配置。
- 更新插件作者列表。
- 更新 README 多语言链接和项目描述。
- 更新 LICENSE copyright 信息。

### 修复

- 修复角色模板纹理展开和拉伸模式配置。
- 修复部分文档路径、图片导入路径和侧边栏生成配置。

### 移除

- 移除旧版 Inspector 中不再使用的 shots 编辑器插件文件。
- 移除演员缩放、镜像和纵向位置相关旧参数，角色显示与移动改为基于横向网格位置。
- 移除已过时的 `docs/about`、旧 `docs/script`、旧 `docs/tutorial` 等文档目录。
- 移除西班牙语和法语 README 链接及对应 README 文件。
- 移除旧版 `assets/kona/1.0` 立绘资源。

### 兼容性提示

- 2.4.0 调整了演员定位模型，旧脚本中依赖 `actor show ... at <x> <y> scale <value> [mirror]` 的写法需要迁移到新的网格定位方式。
- 变量系统分为持久变量 `%` 和临时变量 `$`：持久变量会进入存档，临时变量只在当前流程中使用。
- WebTool 仅在 Web 平台启用，其他平台不会注入浏览器快捷键处理逻辑。
